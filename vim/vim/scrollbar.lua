--require("scrollbar").setup({
--  handle = {
--    color = "#808080",
--  },
--  show_in_active_only = true,
--})

require('scrollview').setup({
  excluded_filetypes = {'NvimTree'},
  current_only = true,
})
